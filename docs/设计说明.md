# 《模拟中世纪领主·铁冠之路》设计说明

## 一句话

父亲远征失败后，玩家继承渡鸦堡。曾臣服于父亲的领主们在他死后各自独立，玩家要把他们重新纳入渡鸦家的旗下。

## 核心循环

经营产出 → 养活人口与军队 → 收服叛臣（打服 / 说服 / 收买）→ 处理战损与战后统治 → 新领地扩大产出与边境 → 更大的冬季、功臣与反攻压力。

## 三条收服路线

- **打服**：攻城胜利后守将被俘，四选一处置。见效确定，但有战损，且处死会大幅拉低正统性。
- **说服**：派使者累积个人好感，配合王室正统性与邻近压力压低抵抗，成功后辖地与骑士一并带过来，无战损。
- **收买**：金币加封地承诺，见效最快，代价是正统性与忠诚基线，承诺未兑现会翻倍反噬。

路线效力因人而异。摄政公爵是篡位者，唯一只能打；布兰只服强者；伊莎贝尔在正统性够高时会主动回归。

> 当前进度：打服已实现（P2a），说服与收买属 P2b。

## 王室正统性

不直接解锁任何东西，只降低所有说服的难度：

```
说服阻力 = defiance − (正统性 × 0.6 + 好感 × 0.8 + 邻近压力 × 0.5) × routes.persuade
```

邻近压力让打仗与说服互相供能：打下他的邻居会让他更容易谈。而屠城式扩张堆邻近压力最快，却持续拉低正统性，最终反噬后面所有人。

## 数据约定

时间只有一个来源：`clock.elapsedMs`。季节、年份、`turn` 全部由它派生，世界推进靠 `s.timers` 里各自到期的计时器驱动，与任务队列同构，因此在线推进与离线补算复用同一个循环。

`territory.lordId` 是领主归属的唯一真相源，领主辖地由 territories 派生，**不单独存储**——这样不可能出现「领主以为自己有这块地，但地图上它属于别人」的双向不同步。

骑士的初始归属写在 `LORD_DEFS[].knights`，当前归属存在 `knight.liegeLordId`。归属变更时 `side` 与 `liegeLordId` 必须同进同退，测试锁定了这条不变式。

## 实现阶段

- **P0** 清理回合制遗留（已完成）
- **P2a** 叛臣骨架与打服路线（已完成）
- **P1a** 实时化骨架：地图邻接修复、时钟改 elapsedMs、统一调度器、冷却取代 seasonLocks、离线放宽到 2 小时（已完成）
- **P1b** 连续化（知识/损耗/守军漂移/危机按时长）、加冕倒计时、王冠谷开城条件、研究队列并发
- **P2b** 说服与收买路线、正统性接线、将领页重做
- **P3** 地图分层与扩张、AI 决策质量
- **P4** 数值配平

详细设计见 `docs/superpowers/specs/2026-08-09-rebel-lords-design.md`，实现计划见 `docs/superpowers/plans/2026-08-09-p2a-rebel-lords-skeleton.md`。
